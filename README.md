# Cpp_Webserver
Non-blocking, single-poll event-driven HTTP server. Supports GET, POST, DELETE, CGI (PHP/Python), chunked transfer decoding, virtual hosts, and NGINX-inspired configuration with no external libraries.
